{
  "name": "pyramid",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel serve index.html --dist-dir bin-dev",
    "build": "parcel build index.html --public-url \"./\" -d bin --no-source-maps",
    "tswatch": "tsc --watch --noEmit --project tsconfig.json",
    "tslint": "tslint --project .",
    "tslintfix": "tslint --project . --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pure-rand": "^6.0.0"
  },
  "devDependencies": {
    "parcel": "^2.0.0",
    "@parcel/transformer-sass": "^2.0.0",
    "rimraf": "^5.0.0",
    "sass": "^1.62.1",
    "typescript": "^5.0.0"
  }
}
